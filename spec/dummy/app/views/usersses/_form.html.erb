<%= form_with(model: userss) do |form| %>
  <% if userss.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(userss.errors.count, "error") %> prohibited this userss from being saved:</h2>

      <ul>
        <% userss.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
