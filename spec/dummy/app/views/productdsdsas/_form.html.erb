<%= form_with(model: productdsdsa) do |form| %>
  <% if productdsdsa.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(productdsdsa.errors.count, "error") %> prohibited this productdsdsa from being saved:</h2>

      <ul>
        <% productdsdsa.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
