<%= form_with(model: userssd) do |form| %>
  <% if userssd.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(userssd.errors.count, "error") %> prohibited this userssd from being saved:</h2>

      <ul>
        <% userssd.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
