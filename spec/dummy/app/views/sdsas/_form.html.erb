<%= form_with(model: sdsa) do |form| %>
  <% if sdsa.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sdsa.errors.count, "error") %> prohibited this sdsa from being saved:</h2>

      <ul>
        <% sdsa.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
