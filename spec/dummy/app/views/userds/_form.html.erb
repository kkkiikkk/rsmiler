<%= form_with(model: userd) do |form| %>
  <% if userd.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(userd.errors.count, "error") %> prohibited this userd from being saved:</h2>

      <ul>
        <% userd.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
